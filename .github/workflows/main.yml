name: Test Hello World

on:
   pull_request:
    branches:
    - main

jobs:

  test:
    runs-on: "composite"
    steps:
    - name: run a multi script
      run: |
         echo Hello world!1
         echo Hello world!2
         shell: bash
         
    - name: List directory
      run: ls -la
      
    - name: script.sh file
      run:  bash ./script.sh
